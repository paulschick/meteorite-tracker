import { FormatMassPipe } from './format-mass.pipe';

describe('FormatMassPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMassPipe();
    expect(pipe).toBeTruthy();
  });
});
